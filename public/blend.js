(function () {
    'use strict';
    var state = { room: null };
    function esc(value) { return typeof es === 'function' ? es(value) : String(value || '').replace(/[&<>"']/g, function (c) { return ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' })[c]; }); }
    function escJs(value) { return typeof esJs === 'function' ? esJs(value) : String(value || '').replace(/\\/g, '\\\\').replace(/'/g, "\\'"); }
    function cleanSong(song) { var id = String(song && (song.videoId || song.id) || '').trim(); return id ? { id: id, videoId: id, title: String(song.title || 'Lagu').slice(0, 180), artist: String(song.artist || 'MalaMusic').slice(0, 120), cover: String(song.cover || '').slice(0, 700), ytUrl: String(song.ytUrl || '').slice(0, 700) } : null; }
    function api(action, options) { options = options || {}; var method = options.method || 'GET'; var url = '/api/blend?action=' + encodeURIComponent(action) + (options.room ? '&room=' + encodeURIComponent(options.room) : ''); return fetch(url, { method: method, headers: method === 'GET' ? {} : { 'Content-Type': 'application/json' }, body: method === 'GET' ? undefined : JSON.stringify(options.body || {}) }).then(function (r) { return r.json().then(function (d) { if (!r.ok || d.status === false) throw new Error(d.message || 'Blend gagal diproses.'); return d; }); }); }
    function closeModal() { var m = document.getElementById('blend-modal'); if (m) m.remove(); }
    function renderRoom(room, inviteCode) {
        state.room = room; closeModal(); var songs = (room.playlist && room.playlist.songs) || [], members = room.members || [];
        var m = document.createElement('div'); m.id = 'blend-modal'; m.className = 'fixed inset-0 z-[430] flex items-end sm:items-center justify-center bg-black/75 px-0 sm:px-4';
        m.onclick = function (e) { if (e.target === m) closeModal(); };
        m.innerHTML = '<div class="w-full sm:max-w-xl max-h-[90vh] overflow-hidden rounded-t-3xl sm:rounded-3xl bg-[#15151b] border border-white/10 shadow-2xl">' +
            '<div class="p-5 border-b border-white/10 flex items-start gap-3"><div class="w-12 h-12 rounded-2xl bg-fuchsia-500/15 text-fuchsia-200 flex items-center justify-center shrink-0"><i data-lucide="sparkles" class="w-6 h-6"></i></div><div class="min-w-0 flex-1"><p class="text-[10px] uppercase tracking-[.18em] text-fuchsia-200/80 font-black">Blend Kolaboratif</p><h2 class="text-xl font-black text-white mt-1 truncate">'+esc(room.title || 'Blend MalaMusic')+'</h2><p class="text-xs text-white/50 mt-1">'+members.length+' anggota · '+songs.length+' lagu relevan</p></div><button onclick="Blend.close()" class="w-9 h-9 rounded-full bg-white/10 text-white">×</button></div>' +
            '<div class="p-4 border-b border-white/10 flex flex-wrap gap-2">'+members.map(function (member) { return '<span class="px-3 py-1.5 rounded-full bg-white/[.06] border border-white/10 text-xs text-white/75">'+esc(member.name)+(member.role === 'owner' ? ' · pemilik' : '')+'</span>'; }).join('')+'</div>' +
            '<div class="p-4 flex gap-2"><button onclick="Blend.copyInvite(\''+escJs(inviteCode || room.id)+'\')" class="flex-1 rounded-xl bg-fuchsia-400/15 border border-fuchsia-300/30 text-fuchsia-100 py-3 text-xs font-bold"><i data-lucide="link" class="w-4 h-4 inline mr-1"></i>Salin kode undangan</button><button onclick="Blend.refresh(\''+escJs(room.id)+'\')" class="w-11 rounded-xl bg-white/10 border border-white/10 text-white"><i data-lucide="refresh-cw" class="w-4 h-4 inline"></i></button></div>' +
            '<div class="max-h-[46vh] overflow-y-auto px-4 pb-4">'+(songs.length ? songs.map(function (song, i) { return '<button onclick="Blend.play('+i+')" class="w-full flex items-center gap-3 p-3 rounded-xl hover:bg-white/10 text-left"><img src="'+(typeof safeMediaUrl === 'function' ? safeMediaUrl(song.cover || (typeof FI !== 'undefined' ? FI : ''), typeof FI !== 'undefined' ? FI : '') : (song.cover || ''))+'" class="w-11 h-11 rounded-lg object-cover" onerror="this.src=\''+(typeof FI !== 'undefined' ? FI : '')+'\'" /><span class="min-w-0 flex-1"><strong class="block text-sm text-white truncate">'+esc(song.title)+'</strong><span class="block text-xs text-white/50 truncate">'+esc(song.artist)+'</span></span></button>'; }).join('') : '<p class="text-center text-sm text-white/50 py-12">Belum ada lagu. Tambahkan lagu dari hasil pencarian.</p>')+'</div></div>';
        document.body.appendChild(m); if (window.lucide) lucide.createIcons();
    }
    window.Blend = {
        close: closeModal,
        create: function (partnerUid, partnerName) { api('create', { method: 'POST', body: { partnerUid: partnerUid } }).then(function (d) { renderRoom(d.room, d.inviteCode); if (typeof showToast === 'function') showToast('Blend dibuat untuk kamu dan ' + (partnerName || 'temanmu')); }).catch(function (e) { if (typeof showToast === 'function') showToast(e.message); }); },
        join: function (roomId) { api('join', { method: 'POST', room: roomId, body: { roomId: roomId } }).then(function (d) { renderRoom(d.room, roomId); }).catch(function (e) { if (typeof showToast === 'function') showToast(e.message); }); },
        promptJoin: function (roomId) { api('state', { room: roomId }).then(function (d) { var room = d.room || {}; var m = document.createElement('div'); m.id = 'blend-modal'; m.className = 'fixed inset-0 z-[430] flex items-end sm:items-center justify-center bg-black/75 px-0 sm:px-4'; m.innerHTML = '<div class="w-full sm:max-w-md rounded-t-3xl sm:rounded-3xl bg-[#15151b] border border-white/10 shadow-2xl p-6"><p class="text-[10px] uppercase tracking-[.18em] text-fuchsia-200/80 font-black">Undangan Blend</p><h2 class="text-xl font-black text-white mt-2">'+esc(room.title || 'Blend MalaMusic')+'</h2><p class="text-sm text-white/60 mt-2">Kamu diundang untuk menggabungkan selera musik dan membuat playlist kolaboratif.</p><div class="flex gap-2 mt-6"><button onclick="Blend.close()" class="flex-1 rounded-xl bg-white/10 text-white py-3 text-xs font-bold">Nanti</button><button onclick="Blend.join(\''+escJs(roomId)+'\')" class="flex-1 rounded-xl bg-fuchsia-400 text-black py-3 text-xs font-black">Gabung Blend</button></div></div>'; document.body.appendChild(m); }).catch(function (e) { if (typeof showToast === 'function') showToast(e.message); }); },
        refresh: function (roomId) { api('state', { room: roomId }).then(function (d) { renderRoom(d.room, roomId); }).catch(function (e) { if (typeof showToast === 'function') showToast(e.message); }); },
        copyInvite: function (code) { var text = location.origin + '/blend/' + encodeURIComponent(code); if (navigator.clipboard) navigator.clipboard.writeText(text).then(function () { if (typeof showToast === 'function') showToast('Link Blend disalin.'); }); else if (typeof showToast === 'function') showToast('Kode Blend: ' + code); },
        play: function (index) { var songs = state.room && state.room.playlist && state.room.playlist.songs || []; var song = cleanSong(songs[index]); if (!song) return; closeModal(); if (typeof S !== 'undefined') { S.pl = songs; S.pi = index; S.ps = 'blend'; S.ct = song; if (typeof UU === 'function') UU(); if (typeof MP !== 'undefined' && MP.show) MP.show(); S.il = true; if (typeof UB === 'function') UB(); if (typeof resetLyricsUI === 'function') resetLyricsUI(song.videoId); if (typeof loadTrack === 'function') loadTrack(song); } },
        active: function () { return Boolean(state.room && state.room.id); },
        invite: function (partnerUid, partnerName) { if (!state.room) return; api('invite', { method: 'POST', room: state.room.id, body: { roomId: state.room.id, partnerUid: partnerUid } }).then(function (d) { renderRoom(d.room, state.room.id); if (typeof showToast === 'function') showToast((partnerName || 'Pengguna') + ' diundang ke Blend.'); }).catch(function (e) { if (typeof showToast === 'function') showToast(e.message); }); },
        addSong: function (song) { if (!state.room) return; api('add', { method: 'POST', room: state.room.id, body: { roomId: state.room.id, song: song } }).then(function (d) { state.room = d.room; if (typeof showToast === 'function') showToast('Lagu ditambahkan ke Blend.'); }).catch(function (e) { if (typeof showToast === 'function') showToast(e.message); }); },
        openFromUser: function (uid, name) { if (Blend.active()) { var inviteText = 'Undang ' + (name || 'pengguna ini') + ' ke Blend yang sedang aktif?'; if (window.confirm && !window.confirm(inviteText)) return; Blend.invite(uid, name); return; } var confirmText = 'Buat Blend dengan ' + (name || 'pengguna ini') + '?'; if (window.confirm && !window.confirm(confirmText)) return; Blend.create(uid, name); }
    };
}());
